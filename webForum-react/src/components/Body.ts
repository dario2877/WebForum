import { react } from "react";
import { comments } from "../assets/commentData";



type BodyProps = {
    listOfObjects: typeof comments
  
};
export function Body(props: BodyProps) {
  return (


);
}